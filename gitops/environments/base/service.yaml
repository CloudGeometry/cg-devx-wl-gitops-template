apiVersion: v1
kind: Service
metadata:
  name: <WL_SERVICE_NAME>
spec:
  type: ClusterIP
  selector:
    app: "<WL_SERVICE_NAME>-app"
  ports:
    - protocol: TCP
      port: 8080
      targetPort: http
      name: http