---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "<WL_SERVICE_NAME>-deployment"
spec:
  template:
    spec:
      containers:
      - name: "<WL_SERVICE_NAME>-container"
        env: []
      